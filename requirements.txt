# CPU-only deploy requirements for the Streamlit app (app.py)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.9.1+cpu
torchvision==0.24.1+cpu

# Pretrained backbone (downloads ViT-B/32 weights on first use)
open_clip_torch>=2.24

# App
streamlit>=1.38
pillow>=11.0