--extra-index-url https://download.pytorch.org/whl/cu124

# CUDA build — matched pair, forced from the PyTorch index above so no
# CPU-only wheel can slip in as a transitive dependency.
torch==2.6.0+cu124
torchvision==0.21.0+cu124

# Pretrained backbone (downloads ViT-B/32 weights on first use)
open_clip_torch>=2.24

# Training head + eval + logging
numpy>=2.1
pillow>=11.0
scikit-learn>=1.5
tensorboard>=2.18

# Step 6 embedding visualization
matplotlib>=3.11